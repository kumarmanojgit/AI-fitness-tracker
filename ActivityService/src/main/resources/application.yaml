spring:
  application:
    name: ActivityService

  datasource:
    url: jdbc:mysql://localhost:3306/fitnessactivity
    username: root
    password: Manoj@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8081